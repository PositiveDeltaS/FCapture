function Shellbags
{
    # Get array of User Profile names
    #$User = $(Get-ChildItem "$($env:systemdrive)\Users\").Name 

    # Get ntuser.dat and usrclass.dat info for each user
    #Foreach ($name in $User)
    #{
        #Save-NTUSER $name
        #Save-USRCLASS $name
    #}
}

#function Save-NTUSER([string]$name)
#{}

#function Save-USRCLASS([string]$name)
#{}